import "@/public/css/reset.css";

export const metadata = {
  title: "Earth Our Home",
  description: "Blog about our home, Earth",
};

export default function ClientLayout({ children }) {
  return children;
}
